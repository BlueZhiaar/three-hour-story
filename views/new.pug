extends layout

block content
  h2 新規キャラクター作成
  form(method="post" action="/character")
    input(type="hidden" name="_csrf" value!=csrfToken)
    div.form-group 
      input(type="text" name="charaName").form-control
    div.form-group  
      br
      button(type="submit" onclick="OnButtonClick();").btn.btn-primary 登録完了