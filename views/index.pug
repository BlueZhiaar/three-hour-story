extends layout

block content
    h1= title
    div 
      if user 
        div
          a(href="/character/new").btn.btn-primary 新しいキャラクターを作る
          br
          div.my-3
            a(href="/logout").btn.btn-info #{user.username} をログアウト
          - var hasCharacter = characterdatas.length > 0
          if hasCharacter 
            h6 あなたのキャラクター
              br
              each characterdata in characterdatas
                a(href=`/character/${characterdata.character_id}/archive`).card #{characterdata.character_name} #{characterdata.updatedAt}
                hr
                br
      else 
        div 
          a(href="/login").btn.btn-info ログイン
          p 物語りを紡ぎます。ログインして主人公に名前をつけてください。