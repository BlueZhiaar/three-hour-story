extends layout

block content
  h1= title
  p Welcome to #{title}
  if user 
    p Hello, #{user.username} 
    a(href="/newchara") Make New Charactor 
    br
    br
    a(href="/logout") Logout 
  else 
    a(href="/login") Login 
