extends layout 

block content 
  div.border.border-dark
    h2 #{cn}さんの物語
      each episode_body in  episode_bodys
        p.card #{episode_body}
        hr 
      p 結末 #{endingstr}
    a(href='/').btn.btn-secondary キャラクター一覧に戻る